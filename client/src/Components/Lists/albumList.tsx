import { defaultIconPrefixCls } from "antd/lib/config-provider";
import { IAlbum } from "Data/DataTypes";
import React from 'react';
import { getAllAlbums } from "Services/Album";

const AlbumList: React.FC = (props) => {

  //console.log("albums", props.albums)
  //const albumNameArray = albums.map(album => album.name);

  return (
    <div>
    </div>
  )
}

export default AlbumList;