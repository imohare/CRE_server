import { Loader, LoadingManager } from 'three';
import * as LAMINA from '../vanilla';
export declare class LaminaLoader extends Loader {
    constructor(manager?: LoadingManager);
    load(url: string, onLoad?: (event: LAMINA.LayerMaterial) => void, onError?: (event: Error) => void): null | undefined;
    loadAsync(url: string): Promise<LAMINA.LayerMaterial>;
}
