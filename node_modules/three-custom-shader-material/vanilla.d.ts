import { IUniform, Material } from 'three';
import { AllMaterialParams, iCSMProps } from './types';
export default class CustomShaderMaterial extends Material {
    uniforms: {
        [key: string]: IUniform<any>;
    };
    private base;
    constructor(baseMaterial: new () => Material, fragmentShader?: string, vertexShader?: string, uniforms?: {
        [key: string]: THREE.IUniform<any>;
    }, opts?: AllMaterialParams);
    update(fragmentShader: iCSMProps['fragmentShader'], vertexShader: iCSMProps['vertexShader'], uniforms: iCSMProps['uniforms']): void;
    private generateMaterial;
    private patchShader;
    private parseShader;
}
